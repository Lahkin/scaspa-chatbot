"""Agent construction.

Not implemented yet. Will build the agent with LangChain v1's
`from langchain.agents import create_agent` (LangGraph runtime).
`create_react_agent` is the removed v0 entry point — do not use it."""
