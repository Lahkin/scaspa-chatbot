/**
 * TypeScript mirrors of the backend's Pydantic schemas.
 *
 * Not implemented yet. These will mirror `docs/api-contract.md` exactly —
 * ChatRequest, ChatResponse, Citation, ChartSpec, ToolCall, ResponseMeta,
 * ErrorEnvelope — and must be kept in step with it. If the contract and these
 * types disagree, the contract wins and the mismatch is raised with the backend
 * team rather than worked around here.
 */
export {};
