/**
 * Every fetch call in the application lives in this file — CLAUDE.md rule 7.
 *
 * Not implemented yet. Keeping the network surface in one module is what makes
 * it possible to state, and check, that there is no Authorization header, no
 * cookie, and no call to any host but the configured backend.
 */
export {};
