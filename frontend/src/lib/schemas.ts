/**
 * Zod schemas: runtime validation at the network boundary.
 *
 * Not implemented yet. CLAUDE.md rule 8 — every backend response is parsed
 * through a schema before it reaches state. TypeScript types are erased at
 * runtime and prove nothing about what actually arrived over the wire.
 */
export {};
